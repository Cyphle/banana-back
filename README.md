# Banana - Easy account manager

## TypeORM
- Create migration: `npx typeorm migration:create ./migrations/<name>`
- Run migration: `npm run typeorm migration:run -- -d ./src/config/data-source.config.ts`